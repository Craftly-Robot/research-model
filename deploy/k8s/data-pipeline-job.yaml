apiVersion: batch/v1
kind: Job
metadata:
  name: craftly-data-pipeline
spec:
  backoffLimit: 1
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: pipeline
          image: craftly-api:latest
          imagePullPolicy: IfNotPresent
          command:
            - python
            - -m
            - src.craftly.learning.data_pipeline
          args:
            - --sources
            - config/data_sources.ultimate.json
            - --dataset-id
            - craftly-defensive-coding-corpus
            - --work-dir
            - /data/pipeline
            - --frontier-backend
            - postgres
            - --postgres-dsn
            - "$(CRAFTLY_DATABASE_URL)"
            - --object-store-uri
            - s3://craftly-datasets/pretraining
            - --object-store-endpoint-url
            - http://craftly-minio:9000
            - --max-docs
            - "1000000"
            - --workers
            - "64"
            - --max-depth
            - "2"
            - --vocab-size
            - "64000"
            - --sequence-length
            - "2048"
            - --shard-token-count
            - "1000000"
            - --skip-train
          env:
            - name: CRAFTLY_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: craftly-secrets
                  key: database-url
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: craftly-secrets
                  key: minio-root-user
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: craftly-secrets
                  key: minio-root-password
            - name: AWS_EC2_METADATA_DISABLED
              value: "true"
          volumeMounts:
            - name: data-artifacts
              mountPath: /data
          resources:
            requests:
              cpu: "4"
              memory: 16Gi
            limits:
              cpu: "16"
              memory: 64Gi
      volumes:
        - name: data-artifacts
          persistentVolumeClaim:
            claimName: craftly-data-artifacts

