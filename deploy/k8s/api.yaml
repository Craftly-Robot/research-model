apiVersion: apps/v1
kind: Deployment
metadata:
  name: craftly-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: craftly-api
  template:
    metadata:
      labels:
        app: craftly-api
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: api
          image: craftly-api:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8090
          env:
            - name: CRAFTLY_ENV
              value: production
            - name: CRAFTLY_AUTH_ENABLED
              value: "1"
            - name: CRAFTLY_QUOTA_ENABLED
              value: "1"
            - name: CRAFTLY_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: craftly-secrets
                  key: jwt-secret
            - name: CRAFTLY_WORKSPACE_BOOTSTRAP_TOKEN
              valueFrom:
                secretKeyRef:
                  name: craftly-secrets
                  key: workspace-bootstrap-token
            - name: CRAFTLY_ALLOW_TOKEN_ISSUE
              value: "0"
            - name: CRAFTLY_PROJECT_ROOTS
              value: /workspace/projects
            - name: CRAFTLY_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: craftly-secrets
                  key: database-url
            - name: CRAFTLY_REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: craftly-secrets
                  key: redis-url
            - name: CRAFTLY_QDRANT_URL
              value: http://craftly-qdrant:6333
            - name: CRAFTLY_EMBEDDING_URL
              valueFrom:
                secretKeyRef:
                  name: craftly-secrets
                  key: embedding-url
            - name: CRAFTLY_OBJECT_STORE_URI
              value: s3://craftly-datasets/training-workspace
            - name: CRAFTLY_OBJECT_STORE_ENDPOINT_URL
              value: http://craftly-minio:9000
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: craftly-secrets
                  key: minio-root-user
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: craftly-secrets
                  key: minio-root-password
            - name: AWS_EC2_METADATA_DISABLED
              value: "true"
            - name: CRAFTLY_TRAINING_GIT_COMMIT
              valueFrom:
                configMapKeyRef:
                  name: craftly-training-build
                  key: git-commit
            - name: CRAFTLY_TRAINING_IMAGE_DIGEST
              valueFrom:
                configMapKeyRef:
                  name: craftly-training-build
                  key: image-digest
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: runtime
              mountPath: /app/artifacts
            - name: tmp
              mountPath: /tmp
            - name: project-workspaces
              mountPath: /workspace/projects
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 8090
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health/ready
              port: 8090
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: "500m"
              memory: 1Gi
            limits:
              cpu: "2"
              memory: 4Gi
      volumes:
        - name: runtime
          emptyDir:
            sizeLimit: 2Gi
        - name: tmp
          emptyDir:
            medium: Memory
            sizeLimit: 128Mi
        - name: project-workspaces
          persistentVolumeClaim:
            claimName: craftly-project-workspaces
---
apiVersion: v1
kind: Service
metadata:
  name: craftly-api
spec:
  selector:
    app: craftly-api
  ports:
    - port: 80
      targetPort: 8090
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: craftly-training-build
data:
  git-commit: "replace-with-immutable-git-commit"
  image-digest: "replace-with-sha256-image-digest"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: craftly-project-workspaces
spec:
  accessModes: ["ReadWriteMany"]
  storageClassName: craftly-workspaces
  resources:
    requests:
      storage: 1Ti

