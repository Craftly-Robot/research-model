apiVersion: apps/v1
kind: Deployment
metadata:
  name: craftly-data-worker
spec:
  replicas: 4
  selector:
    matchLabels:
      app: craftly-data-worker
  template:
    metadata:
      labels:
        app: craftly-data-worker
    spec:
      containers:
        - name: crawler
          image: craftly-api:latest
          imagePullPolicy: IfNotPresent
          command: ["python", "-m", "src.craftly.learning.worker"]
          env:
            - name: CRAFTLY_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: craftly-secrets
                  key: database-url
            - name: CRAFTLY_DATA_SOURCES
              value: config/data_sources.ultimate.json
            - name: CRAFTLY_RAW_OUTPUT_DIR
              value: /data/raw
            - name: CRAFTLY_CLEAN_OUTPUT_DIR
              value: /data/clean
            - name: CRAFTLY_ASYNC_WORKERS
              value: "32"
            - name: CRAFTLY_BATCH_DOCS
              value: "10000"
            - name: CRAFTLY_WORKER_LOOPS
              value: "100"
          volumeMounts:
            - name: data-artifacts
              mountPath: /data
          resources:
            requests:
              cpu: "1"
              memory: 1Gi
            limits:
              cpu: "4"
              memory: 4Gi
      volumes:
        - name: data-artifacts
          persistentVolumeClaim:
            claimName: craftly-data-artifacts
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: craftly-data-artifacts
spec:
  accessModes: ["ReadWriteMany"]
  resources:
    requests:
      storage: 1Ti

