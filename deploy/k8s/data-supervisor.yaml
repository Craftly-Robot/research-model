apiVersion: apps/v1
kind: Deployment
metadata:
  name: craftly-data-supervisor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: craftly-data-supervisor
  template:
    metadata:
      labels:
        app: craftly-data-supervisor
    spec:
      containers:
        - name: supervisor
          image: craftly-api:latest
          imagePullPolicy: IfNotPresent
          command: ["python", "-m", "src.craftly.learning.supervisor"]
          env:
            - name: CRAFTLY_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: craftly-secrets
                  key: database-url
            - name: CRAFTLY_DATA_SOURCES
              value: config/data_sources.ultimate.json
            - name: CRAFTLY_RAW_OUTPUT_DIR
              value: /data/raw
            - name: CRAFTLY_CLEAN_OUTPUT_DIR
              value: /data/clean
            - name: CRAFTLY_OBJECT_STORE_URI
              value: s3://craftly-datasets/pretraining
            - name: CRAFTLY_WORKER_REPLICAS
              value: "4"
            - name: CRAFTLY_ASYNC_WORKERS
              value: "32"
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: craftly-secrets
                  key: minio-root-user
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: craftly-secrets
                  key: minio-root-password
            - name: AWS_EC2_METADATA_DISABLED
              value: "true"
          volumeMounts:
            - name: data-artifacts
              mountPath: /data
          resources:
            requests:
              cpu: "1"
              memory: 2Gi
            limits:
              cpu: "4"
              memory: 8Gi
      volumes:
        - name: data-artifacts
          persistentVolumeClaim:
            claimName: craftly-data-artifacts

