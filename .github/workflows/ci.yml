name: craftly-ci

permissions:
  contents: read

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-local-dev.txt
          pip install --upgrade bandit semgrep pip-audit
          pip install -e . --no-deps
      - name: Client package smoke
        run: |
          craftly --help
          python -c "from craftly_client import Workspace; assert Workspace.__name__ == 'Workspace'"
      - name: Compile
        run: python -m compileall -q src/craftly tests
      - name: Release gate
        run: python -m src.craftly.evaluation.release_gate
      - name: Production hardening gates
        run: |
          python -m src.craftly.deployment.k8s_validate --output-dir artifacts/craftly/ci-k8s-validation
          python -m src.craftly.learning.storage --uri local://artifacts/craftly/ci-object-store --work-dir artifacts/craftly/ci-storage-lifecycle
          mkdir -p artifacts/craftly/ci-dataset
          cat > artifacts/craftly/ci-dataset/clean.jsonl <<'JSONL'
          {"text":"General architecture documentation for Craftly production systems.","license":"mit","category":"general","quality":{"labels":["documentation"]}}
          {"text":"def secure_query(cursor, value): return cursor.execute('select * from users where id=?', [value])","license":"apache-2.0","category":"code","quality":{"labels":["code"]}}
          {"text":"Defensive security guidance: prevent SQL injection with parameterized queries and regression tests.","license":"cc-by-4.0","category":"cybersecurity","quality":{"labels":["defensive_security"]}}
          JSONL
          python -m src.craftly.learning.dataset_validation --inputs artifacts/craftly/ci-dataset/clean.jsonl --output-dir artifacts/craftly/ci-dataset-validation --min-records 3 --min-avg-chars 20
          python -m src.craftly.security.audit --strict-external-tools --no-codeql --output-dir artifacts/craftly/ci-security-audit
      - name: Data platform readiness contract
        run: |
          python -m src.craftly.learning.production_check \
            --sources config/data_sources.ci.json \
            --frontier-backend postgres \
            --postgres-dsn postgresql://user:pass@postgres:5432/craftly \
            --object-store-uri s3://craftly-datasets/pretraining \
            --production \
            --worker-replicas 4 \
            --async-workers 32
          python -m src.craftly.learning.capacity --target-documents 1000000 --worker-replicas 4 --async-workers-per-replica 16
          python -m src.craftly.learning.run_plan \
            --sources config/data_sources.ci.json \
            --output-dir artifacts/craftly/ci-data-run \
            --target-documents 1000 \
            --target-days 1 \
            --postgres-dsn postgresql://user:pass@postgres:5432/craftly \
            --object-store-uri s3://craftly-datasets/pretraining \
            --worker-replicas 4 \
            --async-workers 32
          python -m src.craftly.learning.source_registry --sources config/data_sources.ultimate.json
          python -m src.craftly.learning.governance --store artifacts/craftly/ci-governance report
      - name: Scratch CPU smoke
        run: python -m src.craftly.model_ops.gpu_smoke --device cpu --steps 1 --sequence-length 16 --batch-size 1 --dtype fp32

  workspace-ui:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: apps/training-workspace/package-lock.json
      - name: Install locked dependencies
        working-directory: apps/training-workspace
        run: npm ci
      - name: Production build
        working-directory: apps/training-workspace
        run: npm run build

